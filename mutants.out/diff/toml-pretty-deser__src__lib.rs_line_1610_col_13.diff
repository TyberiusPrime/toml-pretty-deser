--- toml-pretty-deser/src/lib.rs
+++ delete match arm TomlValueState::Ok{..} in TomlValue<T>::as_ref
@@ -1602,17 +1602,17 @@
         match self.state {
             TomlValueState::Ok { .. } => self.value,
             _ => None,
         }
     }
 
     pub fn as_ref(&self) -> Option<&T> {
         match self.state {
-            TomlValueState::Ok { .. } => self.value.as_ref(),
+             /* ~ changed by cargo-mutants ~ */
             _ => None,
         }
     }
 
     pub fn unwrap(self) -> T {
         match self.state {
             TomlValueState::Ok { .. } => self.value.unwrap(),
             _ => panic!("Called unwrap on a TomlValue that is not Ok"),
